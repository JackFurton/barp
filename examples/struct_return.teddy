struct Point {
    x,
    y
}

fn make_point(px, py) {
    let p = Point {
        x: px,
        y: py
    };
    return p;
}

fn main() {
    let p = make_point(10, 20);
    print p.x;
    print p.y;
    return 0;
}
