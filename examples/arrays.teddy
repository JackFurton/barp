// Test arrays
fn main() {
    let arr = [10, 20, 30, 40, 50];
    
    print arr[0];
    print arr[2];
    print arr[4];
    
    // Sum the array
    let sum = arr[0] + arr[1] + arr[2] + arr[3] + arr[4];
    print sum;
}
